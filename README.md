

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set OAuth credentials in [.env.local](.env.local) (Google & GitHub client IDs)
3. Run the app:
   `npm run dev`
